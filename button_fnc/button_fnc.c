#include "fnd_btn.h"

// button_thread 함수 안에서
int main(){
    if (code == KEY_VOLUMEUP && value) {
    move_cursor_left();
    }   
    else if (code == KEY_VOLUMEDOWN && value) {
       delete_digit_at_cursor();
    }
}